#include "libIncludes.h"

int main(){

    /*
    while(true){
        if(initReg(true)){
            cout << users[*ptr - 1].getUserId() << endl;
            cout << users[*ptr-1].getPassword() << endl;
        }
    }
    */

    while(initReg(true));

    return 0;
}
